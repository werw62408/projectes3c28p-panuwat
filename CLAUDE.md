# วิธีทำงานกับเจ้าของโปรเจกต์นี้ (SomudTick / สมุดติ๊ก)

ไฟล์นี้ Claude Code อ่านเองทุกครั้งที่เปิดงานในโปรเจกต์นี้ แก้ได้ตลอด ข้อไหนเปลี่ยนก็แก้ที่นี่

## ภาษา
- คุยและรายงานความคืบหน้า: **ภาษาไทย** อธิบายสั้น ตรง ๆ มีเลขขา ค่า และขั้นตอนที่ทำตามได้จริง
- ข้อความบนจอบอร์ด โค้ด และคอมเมนต์ในโค้ด: **ภาษาอังกฤษ** ใช้คำง่าย
- README ของแต่ละเวอร์ชัน: **ภาษาไทย**
- หน้าเว็บในมือถือ (`webpage.h`): ภาษาไทย เหมือนเดิม

## ลำดับการทำงาน (สำคัญที่สุด)
1. **คุยก่อนเสมอ ห้ามเริ่มเขียนโค้ดจนกว่าเจ้าของจะบอกว่า "เริ่ม" / "เริ่มได้"**
   คำถาม ปัญหา หรือไอเดียใหม่: อธิบายสาเหตุ เสนอทางแก้หรือตัวเลือกพร้อมข้อดีข้อเสีย แนะนำตัวเลือกที่ดีที่สุด แล้วถามสิ่งที่ต้องรู้
2. ถ้าเจ้าของส่งรูปหรือวิดีโอ: ดูทีละเฟรม หาว่าผิดตรงไหน แล้วพยายามทำให้อาการเกิดซ้ำในตัวจำลองก่อน
3. ถ้าไม่แน่ใจเรื่องอุปกรณ์ (ขนาด รุ่น การต่อ): ถามก่อน อย่าเดา
4. พอเริ่มงานแล้ว: **ตรวจให้ละเอียด ("เช็คดี ๆ")** รวมบั๊กทุกตัวที่เจอ ไม่ว่าจะเล็กแค่ไหน
5. **ทดสอบในตัวจำลองก่อนส่งงานทุกครั้ง** (`tools/sim` สำหรับเฟิร์มแวร์, `tools/web` สำหรับหน้าเว็บ) เจ้าของไม่อยากเสียเวลาไปเจอบั๊กบนบอร์ดจริง
6. ส่งงานแล้ว: สรุปผล บอกสิ่งที่ยังต้องลองบนบอร์ดจริง แล้ว**คุยก่อนทำรอบต่อไป**

## ทุกเวอร์ชันใหม่ต้องมี
- โฟลเดอร์ใหม่ `SomudTick_vX/SomudTick` (คัดลอกจากเวอร์ชันก่อน) ห้ามแก้โฟลเดอร์เวอร์ชันเก่า
- เปลี่ยนเลขเวอร์ชันในหน้า About (`screen_settings.h`, บรรทัด `line("Version", ...)`)
- README: ส่วน **"ของใหม่ใน vX"** อยู่บนสุด ทุกข้อมี ✅ วิธีเช็ค และ ❌ ถ้าไม่ได้ต้องทำอะไร
- อัปเดตตารางคำศัพท์ (ทุกคำบนจอ → แปลไทย) ใน README
- `SomudTick_merged.bin` แบบ**ไม่เติมเต็ม 16 MB** แฟลชที่ `0x0` (0x0 bootloader, 0x8000 partitions, 0xe000 boot_app0, 0x10000 app)
- **ตารางแบ่งพื้นที่ (partition) ต้องเหมือนเดิม** เทียบไบต์ที่ 0x8000 กับเวอร์ชันก่อน เพื่อให้ log ในเครื่องไม่หาย
- รูปตัวอย่างหน้าจอ `preview_vX*.png` (ตรวจแล้วว่าข้อความไม่ล้น ไม่ถูกตัด ทั้งจอแนวตั้งและแนวนอน)
- ไฟล์ zip ส่งให้เจ้าของ
- commit + push ที่ branch `somudtick-v8-relax-games`
  - author: `werw62408 <kinashyy@gmail.com>`
  - ท้ายข้อความ commit ใส่ `Co-Authored-By:` และ `Claude-Session:` ตามที่ระบบกำหนด

## กฎของเครื่องที่ต้องคงไว้ (ห้ามพัง)
- กันกดในกระเป๋า:
  - ชิป Home/Uni เปลี่ยนได้เฉพาะการกดค้าง
  - บันทึกได้ผ่านปุ่ม +1 เท่านั้น
  - ตอนจอดับ การแตะหรือโยกจอยครั้งแรกแค่ปลุกจอ (จอย: ต้อง**กด**ถึงจะปลุก โยกไม่ปลุก)
  - เวลาปิดจออยู่ใน Settings
- เจ้าของปิดเครื่องด้วยสวิตช์ ไม่ต้องทำระบบปิดเครื่องหรือเตือนแบตเพิ่ม
- เวลา: Wi-Fi/NTP → โมดูลนาฬิกา DS3231 → เวลาจากมือถือ → เวลาที่จำไว้ตอนรีสตาร์ท → เดา (ห้ามเอาอันไหนออก)
- จอย: ใช้ระบบเดียวทุกหน้า (`nav.h`) ไม่มีโหมดแยก ทิศหมุนตามจอ กดค้าง 1 วินาที = ออกจากเกม โยกซ้ายค้าง 1 วินาที = Back
- ข้อมูลผู้ใช้ (log, prefs, การ์ด SD) ห้ามหายเมื่ออัปเดต

## บอร์ดและขา (ES3C28P: ESP32-S3, จอ 2.8" ILI9341V 240×320, ทัช FT6336G)
| อะไร | ขา |
|---|---|
| I2C (ทัช 0x38, ES8311 0x18, DS3231 0x68 และ EEPROM 0x57) | SDA IO16, SCL IO15 |
| จอยสติ๊ก | VRX IO2, VRY IO3, SW IO14 (ไฟ 3.3V เท่านั้น) |
| IR KY-005 (สั่งแอร์ Panasonic) | IO21 |
| ทัช | INT IO17, RST IO18 |
| อื่น ๆ | ไฟจอ IO45, วัดแบต IO9, RGB LED IO42, BOOT IO0 |
| การ์ด SD (SD_MMC) | 38, 40, 39, 41, 48, 47 |
| เสียง | MCLK 4, BCLK 5, DIN 6, WS 7, DOUT 8, เปิดแอมป์ 1 |
| ช่อง UART ที่ว่าง | RXD/TXD (น่าจะ IO44/IO43 ต้องเทียบวงจรก่อนใช้), GND, 5V |
- ทุกโมดูลใช้สัญญาณ 3.3V
- บอกเลขขาทุกครั้งที่ให้ต่ออุปกรณ์ และเตือนเลขที่อยู่ I2C ที่ชนกัน

## เครื่องมือ
- ตัวจำลอง:
  - `cd tools/sim && ./run_tests.sh ../../SomudTick_vX/SomudTick` ต้องขึ้น `ALL TESTS PASSED`
  - เพิ่ม `test_vX.cpp` ทุกเวอร์ชัน
- ถ่ายภาพหน้าจอ: `tools/sim/shots.sh`, รวมภาพด้วย `sheet.py`
- หน้าเว็บ: `node tools/web/test_video.js <webpage.h>`
- สร้าง bin: `tools/build_firmware.sh`
  - core esp32 3.3.12, LovyanGFX 1.2.30, ArduinoJson 7.4.2, IRremoteESP8266
  - FQBN `esp32:esp32:esp32s3:FlashSize=16M,PSRAM=opi,PartitionScheme=huge_app,USBMode=default,CDCOnBoot=default`

## เรื่องที่พักไว้ (อย่าทำจนกว่าจะสั่ง)
- OTA (อัปเดตผ่าน Wi-Fi): น่าสนใจ แต่เก็บไว้ก่อน
- สั่งด้วยเสียง / TinyML
- เซ็นเซอร์ตัวสุดท้ายที่ช่อง UART: กำลังคุย (ตัวเลือก: PM2.5 ซึ่งแนะนำ, GPS, โมดูลเสียง)
- ทรานซิสเตอร์ขับหลอด IR: รอเจ้าของลองระยะยิงแอร์ของ v11.4 ก่อน

## นิสัยการสั่งงานของเจ้าของ
- มีพื้นฐาน ชอบฟีเจอร์ที่ดูล้ำหรือ advanced แต่ต้องใช้ได้จริงทุกวัน
- ถามภาพรวมก่อน แล้วเลือกเป็นข้อ ๆ เช่น "1 เอา 2 ตามแนะนำ 3 ..."
- ส่งรูปหรือวิดีโอจากบอร์ดจริงมาให้ดู: ดูให้ละเอียด และตอบให้ตรงกับที่เห็น
- อยากได้คำตอบที่ทำตามได้ทันที (เลขขา, ขั้นตอน, ✅/❌) มากกว่าทฤษฎี
